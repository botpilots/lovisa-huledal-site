media:
  input: public/media
  output: /media
  rename: safe
  categories: [image]

components:
  socialMedia:
    type: object
    fields:
      - name: facebook
        label: Facebook
        type: string

      - name: instagram
        label: Instagram
        type: string

      - name: youtube
        label: YouTube
        type: string

content:
  # HOME
  - name: home
    label: Home
    type: file
    path: content/home.json
    format: json
    operations:
      delete: false
    fields:
      - name: heroTitle
        label: Hero title
        description: Main name on the landing image (default top left). Offsets (%) position the center of the text in the hero.
        type: object
        fields:
          - name: text
            label: Text
            type: string

          - name: offsetX
            label: Horizontal position — desktop (%)
            description: Text center on viewports 768px and wider (0 = left, 100 = right).
            type: number
            options:
              min: 0
              max: 100

          - name: offsetY
            label: Vertical position — desktop (%)
            description: Text center on viewports 768px and wider (0 = top, 100 = bottom).
            type: number
            options:
              min: 0
              max: 100

          - name: offsetXMobile
            label: Horizontal position — mobile (%)
            description: Text center below 768px width only. Leave empty to use the desktop value.
            type: number
            options:
              min: 0
              max: 100

          - name: offsetYMobile
            label: Vertical position — mobile (%)
            description: Text center below 768px width only. Leave empty to use the desktop value.
            type: number
            options:
              min: 0
              max: 100

      - name: typography
        label: Typography
        description: Google Font family names (e.g. Roboto, Inter, Playfair Display). Weights are optional — the site picks sensible defaults and loads only what each font supports.
        type: object
        fields:
          - name: body
            label: Body text
            description: General page text — biography, sections, contact, etc.
            type: object
            fields:
              - name: family
                label: Font family
                type: string

              - name: weight
                label: Font weight
                description: "100–900. Default 400."
                type: number
                required: false
                options:
                  min: 100
                  max: 900
                  step: 100

              - name: fontSize
                label: Font size (rem)
                description: Optional base text size in rem, e.g. 1 for 1rem.
                type: number
                required: false

          - name: heroTitle
            label: Hero title
            type: object
            fields:
              - name: family
                label: Font family
                type: string

              - name: weight
                label: Font weight
                description: "100–900. Default 300."
                type: number
                required: false
                options:
                  min: 100
                  max: 900
                  step: 100

              - name: fontSize
                label: Font size — all screens (rem)
                description: Optional size in rem. Also used as fallback when mobile or desktop size is omitted.
                type: number
                required: false

              - name: fontSizeMobile
                label: Font size — mobile (rem)
                description: Below 768px. Default uses responsive clamp.
                type: number
                required: false

              - name: fontSizeDesktop
                label: Font size — desktop (rem)
                description: 768px and wider. Default uses responsive clamp.
                type: number
                required: false

          - name: heroSubtitle
            label: Hero subtitle
            type: object
            fields:
              - name: family
                label: Font family
                type: string

              - name: weight
                label: Font weight
                description: "100–900. Default 300 (italic)."
                type: number
                required: false
                options:
                  min: 100
                  max: 900
                  step: 100

              - name: fontSize
                label: Font size — all screens (rem)
                description: Optional size in rem. Also used as fallback when mobile or desktop size is omitted.
                type: number
                required: false

              - name: fontSizeMobile
                label: Font size — mobile (rem)
                description: Below 768px. Default uses responsive clamp.
                type: number
                required: false

              - name: fontSizeDesktop
                label: Font size — desktop (rem)
                description: 768px and wider. Default uses responsive clamp.
                type: number
                required: false

          - name: siteNav
            label: Hero footer / navigation
            type: object
            fields:
              - name: family
                label: Font family
                type: string

              - name: weight
                label: Font weight
                description: "100–900. Default 400."
                type: number
                required: false
                options:
                  min: 100
                  max: 900
                  step: 100

              - name: fontSize
                label: Font size (rem)
                description: "Navigation link size. Default 0.75rem (0.875rem from 640px)."
                type: number
                required: false

      - name: heroSubtitle
        label: Hero subtitle
        description: Subtitle on the landing image (default top right). Offsets (%) position the center of the text in the hero.
        type: object
        fields:
          - name: text
            label: Text
            type: string

          - name: offsetX
            label: Horizontal position — desktop (%)
            description: Text center on viewports 768px and wider (0 = left, 100 = right).
            type: number
            options:
              min: 0
              max: 100

          - name: offsetY
            label: Vertical position — desktop (%)
            description: Text center on viewports 768px and wider (0 = top, 100 = bottom).
            type: number
            options:
              min: 0
              max: 100

          - name: offsetXMobile
            label: Horizontal position — mobile (%)
            description: Text center below 768px width only. Leave empty to use the desktop value.
            type: number
            options:
              min: 0
              max: 100

          - name: offsetYMobile
            label: Vertical position — mobile (%)
            description: Text center below 768px width only. Leave empty to use the desktop value.
            type: number
            options:
              min: 0
              max: 100

      - name: heroImage
        label: Front page image
        description: Full-screen landing photo behind the hero title and subtitle.
        type: object
        fields:
          - name: image
            label: Image
            type: image

          - name: offsetX
            label: Horizontal focus (%) (left = 0, right = 100)
            type: number
            options:
              min: 0
              max: 100

          - name: offsetY
            label: Vertical focus (%) (higher = lower)
            type: number
            options:
              min: 0
              max: 100

      - name: siteIcon
        label: Site icon (favicon)
        description: Circular icon for browser tabs and bookmarks. Uses the same focus and zoom controls as photos.
        type: object
        fields:
          - name: image
            label: Image
            type: image

          - name: offsetX
            label: Horizontal focus (%) (left = 0, right = 100)
            type: number
            options:
              min: 0
              max: 100

          - name: offsetY
            label: Vertical focus (%) (higher = lower)
            type: number
            options:
              min: 0
              max: 100

          - name: zoom
            label: Zoom (%) (100 = default crop, higher = zoom in)
            type: number
            options:
              min: 100
              max: 300

      - name: biography
        label: Biography (Swedish)
        type: rich-text

      - name: biographyEn
        label: Biography (English)
        type: rich-text

      - name: aboutMosaic
        label: About mosaic
        description: Four images in order — large left, top right, bottom right, wide bottom.
        type: object
        list:
          min: 4
          max: 4
          collapsible:
            collapsed: true
            summary: "Image {index}"
        fields:
          - name: image
            label: Image
            type: image

          - name: offsetX
            label: Horizontal focus (%) (left = 0, right = 100)
            type: number
            options:
              min: 0
              max: 100

          - name: offsetY
            label: Vertical focus (%) (higher = lower)
            type: number
            options:
              min: 0
              max: 100

          - name: alt
            label: Alt text
            type: string

  # REPERTOIRE
  - name: repertoire
    label: Repertoire
    type: file
    path: content/repertoire.json
    format: json
    operations:
      delete: false
    fields:
      - name: entries
        label: Repertoire
        type: object
        list:
          collapsible:
            collapsed: true
            summary: "{composer} - {piece}"
        fields:
          - name: type
            label: Type of Music
            type: select
            options:
              values:
                - Opera
                - Chamber
                - Sacral

          - name: composer
            label: Composer
            type: string

          - name: piece
            label: Piece
            type: string

          - name: role
            label: Role
            type: string

  # PROGRAMMES
  - name: programmes
    label: Programmes
    type: collection
    path: content/programmes
    format: json
    filename: "{title}.json"
    view:
      primary: title
    fields:
      - name: title
        label: Title
        type: string

      - name: color
        label: Programme colour
        description: Accent for the title band, open panel tint, events sub-banner, and image carousel. Leave empty to auto-assign from the Scandinavian palette (dark green, red, blue, gold). Hex or rgb, e.g. #345a75.
        type: color

      - name: description
        label: Description
        type: rich-text

      - name: images
        label: Images
        description: Order in the list is the default sequence. Visitors can step through with arrows on the programme image.
        type: object
        list:
          collapsible:
            collapsed: true
            summary: "Image {index}"
        fields:
          - name: image
            label: Image
            type: image

          - name: offsetX
            label: Horizontal focus (%) (left = 0, right = 100)
            type: number
            options:
              min: 0
              max: 100

          - name: offsetY
            label: Vertical focus (%) (higher = lower)
            type: number
            options:
              min: 0
              max: 100

      - name: repertoire
        label: Repertoire
        type: rich-text

  # EVENTS
  - name: events
    label: Events
    type: collection
    path: content/events
    format: json
    filename: "{fields.dates}-{name}.json"
    view:
      primary: name
      sort: [dates]
      default:
        sort: dates
        order: desc
    fields:
      - name: dates
        label: Dates
        description: One or more performance dates. Each date appears as a separate listing on the website.
        type: date
        list:
          min: 1
          collapsible: true

      - name: time
        label: Time of Day
        type: string

      - name: name
        label: Event / performance name
        description: Optional. If empty, the linked programme title is shown. If set, the programme title appears in parentheses after the name on the website.
        type: string
        required: false

      - name: location
        label: Location
        description: Venue or place, e.g. Båstad Kyrka, Confidencen.
        type: string

      - name: description
        label: Description
        type: rich-text

      - name: link
        label: Link
        type: string

      - name: programme
        label: Programme
        type: reference
        options:
          collection: programmes

  # MEDIA GROUP
  - name: mediaSection
    label: Media
    type: group
    items:

      - name: videos
        label: Videos
        type: collection
        path: content/videos
        format: json
        filename: "{title}.json"
        view:
          primary: title
        fields:
          - name: youtubeLink
            label: YouTube Link
            type: string

          - name: title
            label: Title
            type: string

          - name: description
            label: Description
            type: text

      - name: photos
        label: Photos
        type: file
        path: content/photos.json
        format: json
        operations:
          delete: false
        fields:
          - name: portraits
            label: Portraits
            type: object
            list:
              collapsible:
                collapsed: true
                summary: "{caption}"
            fields:
              - name: image
                label: Image
                type: image

              - name: caption
                label: Caption
                type: string

          - name: onStage
            label: On Stage
            type: object
            list:
              collapsible:
                collapsed: true
                summary: "{caption}"
            fields:
              - name: image
                label: Image
                type: image

              - name: caption
                label: Caption
                type: string

  # CONTACT
  - name: contact
    label: Contact
    type: file
    path: content/contact.json
    format: json
    operations:
      delete: false
    fields:
      - name: agency
        label: Agency
        type: object
        fields:
          - name: name
            label: Name
            type: string

          - name: email
            label: Email
            type: string

          - name: phone
            label: Phone
            type: string

      - name: lovisa
        label: Lovisa
        type: object
        fields:
          - name: name
            label: Name
            type: string

          - name: email
            label: Email
            type: string

          - name: phone
            label: Phone
            type: string

      - name: socialMedia
        label: Social Media
        component: socialMedia

actions:
  - name: deploy-site
    label: Deploy Site
    workflow: pages-cms-action.yml